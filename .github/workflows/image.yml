name: Build Docker Images

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]
    # Publish semver tags as releases.
    tags: [ 'v*.*.*' ]

jobs:
  mysql8:
    name: Build Image mysql8
    uses: ./.github/workflows/docker-publish.yml
    with:
      image-name: 'mysql8'
